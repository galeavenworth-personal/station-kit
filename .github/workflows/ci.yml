name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Basic repo sanity
        run: |
          echo "No automated checks configured yet."
          echo "Add tooling-specific checks here when ready."
